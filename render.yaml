# Render Blueprint — creates the API, its PostgreSQL database and its Redis
# (Key Value) instance, and wires DATABASE_URL / REDIS_URL between them.
# Deploy: Render Dashboard → New → Blueprint → pick this repository.

databases:
  - name: timeflow-db
    databaseName: timeflow
    plan: free

services:
  - type: keyvalue
    name: timeflow-redis
    plan: free
    # BullMQ loses jobs if Redis evicts keys, so eviction must be off.
    maxmemoryPolicy: noeviction
    ipAllowList: [] # internal connections only

  - type: web
    name: timeflow-api
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    # `prestart` runs `prisma migrate deploy` first. Migrations run at start
    # because the private network (and so the database) is not reachable
    # during the build.
    startCommand: npm start
    healthCheckPath: /health/live
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: timeflow-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: timeflow-redis
          property: connectionString
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: COOKIE_SECURE
        value: "true"
      - key: UPLOAD_DIR
        value: ./uploads
      # Frontend URL(s) — asked for when the Blueprint is created.
      - key: CORS_ORIGINS
        sync: false
      - key: APP_URL
        sync: false
      # Free instances cannot open outbound SMTP ports, so mail is logged
      # rather than sent. On a paid plan set this to gmail and fill in
      # GMAIL_USER / GMAIL_APP_PASSWORD / EMAIL_ALLOW_REAL_SEND=true.
      - key: EMAIL_PROVIDER
        value: log
      - key: INBOUND_SYNC_ENABLED
        value: "false"

  # The background worker (emails, activity logs, inbox sync). Render has no
  # free plan for workers — uncomment to add one on the Starter plan.
  # - type: worker
  #   name: timeflow-worker
  #   runtime: node
  #   plan: starter
  #   buildCommand: npm ci && npm run build
  #   startCommand: npm run start:worker
  #   envVars:
  #     - fromGroup: ...  # or repeat the envVars from timeflow-api
